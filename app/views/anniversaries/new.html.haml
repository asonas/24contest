%h1 一日を終わらせる

%p 時空を歪ませて「今」を記念日にしましょう！

=form_for(@anniversary) do |f|
  .form-field
    = f.label '記念日名'
    = f.text_field :anniversary
    = f.label '詳細'
    = f.text_area :description, :rows=>'4'
  .submit-field
    = f.submit '一日を終わらせる'

= link_to '一日一覧へ', anniversaries_path
